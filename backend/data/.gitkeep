# Data Directory Structure

This directory stores all data generated by the rivalry research pipeline.

## Structure:

```
data/
├── sources.db              # SQLite database - deduplicated source catalog
├── raw_sources/            # Raw source documents (HTML, PDF, text)
│   └── by_url_hash/        # Organized by hash of source URL
│       ├── <hash1>/
│       │   └── content.{html|pdf|txt}
│       └── <hash2>/
│           └── content.{html|pdf|txt}
└── analyses/               # Rivalry analysis outputs (JSON)
    ├── Q200101_Q164790/    # Entity pair directory
    │   └── analysis.json   # Full analysis with source references
    └── Q935_Q9047/
        └── analysis.json
```

## Purpose:

- **sources.db**: Central database for all sources across all rivalries (prevents duplicate fetches)
- **raw_sources/**: Original content from web pages, papers, news articles for reference/re-analysis
- **analyses/**: Structured JSON outputs for each rivalry pair analyzed

## Note:

This directory is git-ignored. Do not commit data files - they can be regenerated by running the analysis pipeline.

